#include <stdio.h>

main()
{
    int jumlah;
    float harga_per_unit, harga_total;
    jumlah = 10;
    harga_per_unit = 17.5;
    harga_total = jumlah * harga_per_unit;
    printf("Harga total = %f\n", harga_total);
}
